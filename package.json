{
  "type": "module",
  "scripts": {
    "build": "tsc --outDir dist",
    "build:demo": "tsc demo/dashboard/dashboard.ts --outDir dist/demo/dashboard --module esnext --target es2020",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "vitest"
  }
}
